//  // str = address.match(/:\d+/)[0].substring(1)
//         // if(str[0]==0) return res.status(400).send({status:false,message:`${str} this value is invalid`})
//         // x = `:"${str}"`
//         // address = address.replace(/:\d+/, x)
//         // address = parseJSONSafely(address)

//         address = address.split(" ").join("")
//         if ((address.match(/:\d+/)[0][1])==0) {
//             return res.status(400).send({ status: false, message: `${address.match(/:\d+/)} this value is invalid ` })
//         }

